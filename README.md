# Зависимости
Spring boot + WebSocket + RabbitMQ + Docker Compose + PostgreSQL

# Как запустить

*. cd <путь к репозиторию>/docker
*. sh build.sh
*. docker-compose up

# Остановить проект

* sh stop.sh
